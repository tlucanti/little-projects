
#include <Table.hpp>
#include <iostream>
#include <string>
#include <vector>

void parse_table(std::istream &in, std::vector<std::vector<int> > &transition,
		 std::vector<std::vector<int> > &exit)
{
	int rows = 0
	std::string line;

	while (true) {
		std::getline(in, line);
	}
}
